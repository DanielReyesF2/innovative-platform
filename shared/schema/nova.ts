// Nova connector doesn't need its own tables — conversations are stored in Nova AI's database.
// This file exists for consistency with the module structure.
// Exporting empty object to satisfy module resolution.
export {};
