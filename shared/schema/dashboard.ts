// Dashboard schema — extend per project
// This file is a placeholder that projects customize with their own metrics tables

// Example: A project with KPIs would add kpis and kpi_values tables here
// Example: A project with sales would add sales_metrics tables here

// Re-export common types that dashboard components might need
export type { User, Company } from "./common";
